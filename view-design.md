# 右侧内容区域联动设计

## 触发器（左侧能点击的项）

### 工作台模式：
1. ＋ 新任务（顶部加号图标）
2. 🧩 技能（菜单项）
3. 📊 数据看板（菜单项）
4. 历史对话列表项 × 5 条
5. 定时任务列表项 × 4 条

### 智能体模式：
6. 智能开票专员（智能体项）
7. 风险预警官（智能体项）
8. 经营驾驶舱（智能体项）
9. 能力中心、📚 知识库、🤖 智能体管理（工具项）

## 右侧内容视图设计

### 视图 A：智能体工作区（复用现有驾驶舱）
- 智能开票专员 / 风险预警官 / 经营驾驶舱 → 显示对应驾驶舱
- 数据看板 → 显示驾驶舱变体（全链路 + 数据卡片）
- 能力中心 → 显示「智能体能力池」表格

### 视图 B：对话视图（历史对话）
- 点击某条历史对话 → 右侧显示对话内容
- 顶部 toolbar：对话标题 + 时间 + 状态
- 主区：消息流（用户 / AI 交替气泡）
- 底部：输入框 + 发送按钮

### 视图 C：任务详情（新建任务 / 定时任务）
- 顶部 toolbar：任务名 + 状态徽章
- 主区：任务详情卡（描述、目标、进度）+ 子任务列表 + 操作按钮

### 视图 D：技能列表
- 顶部 toolbar：「技能」 + 「+ 安装新技能」按钮
- 主区：技能卡片网格（图标 + 名称 + 描述 + 使用次数）

### 视图 E：数据看板
- 顶部 toolbar：「数据看板」
- 主区：4 KPI 卡 + 全链路图（和经营驾驶舱类似但更宏观）

### 视图 F：能力中心 / 知识库 / 智能体管理
- 占位页（后续填充）

## 视图切换机制

```js
// 点击左侧某项 → 切换右侧视图
document.querySelectorAll('[data-view]').forEach(el => {
  el.addEventListener('click', () => {
    const view = el.dataset.view;
    document.querySelectorAll('.view-panel').forEach(p => p.classList.remove('active'));
    document.querySelector(`[data-view-panel="${view}"]`)?.classList.add('active');
  });
});
```

## 默认视图（首次进入）
- 工作台模式 → 默认显示"数据看板"视图（最实用的）
- 智能体模式 → 默认显示"智能开票专员"驾驶舱

## 数据结构

```js
const VIEW_DATA = {
  'work-data': { title: '数据看板', content: '... 4 KPI + 全链路图 ...' },
  'work-new-task': { title: '新建任务', content: '... 任务表单 ...' },
  'work-skill': { title: '技能', content: '... 技能卡片网格 ...' },
  'history-1': { title: '上海永胜包装·主数据确认', content: '... 对话内容 ...' },
  // ... 其他历史对话
  'schedule-1': { title: '每日风险扫描', content: '... 任务详情 ...' },
  // ... 其他定时任务
  'agent-billing': { title: '智能开票专员', content: '... 现有驾驶舱 ...' },
  // ... 其他智能体
  'tool-cap': { title: '能力中心', content: '... 占位 ...' },
  // ... 其他工具
};
```

## 实现优先级

**MVP（先做最重要的）**：
1. ✅ 默认视图 = 数据看板（已有）
2. ⭐ 点击历史对话 → 切换到对话视图
3. ⭐ 点击智能体 → 切换到对应驾驶舱
4. ⭐ 点击 + 新任务 → 弹出任务表单

**后续**：
6. 技能 / 定时任务 / 工具的详情页